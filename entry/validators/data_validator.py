# data_validator.py - 数据规范校验器
"""
设计用途：
DataValidator专门负责AgentRequest的数据规范校验，从ConfigCache读取验证规则，
检查请求数据的完整性和格式正确性。验证逻辑聚焦于数据字段本身，无业务逻辑处理。

核心功能：
1. 字段完整性检查：验证必需字段是否存在和非空
2. 格式正确性验证：检查字段格式是否符合规范
3. 类型验证：验证字段数据类型是否正确
4. 业务规则校验：检查字段值是否在合理范围内

验证字段：
- request_id：必需，唯一标识符
- task_type：必需，任务类型
- timestamp：必需，ISO格式时间戳
- intent：可选，但存在时需有效
- user_id、session_id：可选字段的格式验证

设计理念：
- 规则驱动：所有验证规则从配置中读取，便于维护
- 分层验证：先检查存在性，再检查格式，最后检查业务规则
- 错误定位：精确指出哪个字段验证失败，便于前端修复
- 扩展性：支持动态添加新的验证规则，无需修改代码
"""
